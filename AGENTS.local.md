# Local Development Notes for Claude

## Running Tests

To run the UUID extension tests:
```bash
cd ~/build/mysql-test && perl mysql-test-run.pl --suite=/Users/deesix/Source/GitHub/vsql-uuid/test
```

This command runs all tests in the vsql-uuid test suite using the MySQL test framework.
